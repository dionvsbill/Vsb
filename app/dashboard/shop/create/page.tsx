'use client'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'

export default function CreateShop(){const r=useRouter();const [busy,setBusy]=useState(false);const [error,setError]=useState('');
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const res=await fetch('/api/shop',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});const j=await res.json();if(!res.ok)throw new Error(j.error||'Unable to create shop');r.push(`/dashboard/shop/${j.shop.id}`)}catch(err){setError(err instanceof Error?err.message:'Unable to create shop')}finally{setBusy(false)}}
 return <main className="min-h-screen bg-slate-50 p-5 dark:bg-slate-950"><div className="mx-auto max-w-2xl"><h1 className="text-3xl font-black">Create your VSBILL Shop</h1><p className="mt-2 text-slate-500">Create the storefront first. Products and integrations can be configured after approval.</p><form onSubmit={submit} className="glass mt-8 grid gap-4 rounded-2xl p-6">{[['name','Shop name','text'],['slug','Unique slug','text'],['momo_number','MoMo number','text'],['delivery_fee','Delivery fee (GHS)','number'],['jforce_id','JForce ID (optional)','text']].map(([name,label,type])=><label key={name} className="grid gap-2 text-sm font-semibold">{label}<input required={name==='name'||name==='slug'} name={name} type={type} min={type==='number'?0:undefined} step={type==='number'?'0.01':undefined} className="rounded-xl border bg-white px-4 py-3 font-normal outline-none dark:bg-slate-900"/></label>)}<label className="grid gap-2 text-sm font-semibold">Description<textarea name="description" rows={4} className="rounded-xl border bg-white px-4 py-3 font-normal dark:bg-slate-900"/></label>{error&&<p className="rounded-xl border border-red-300 p-3 text-sm text-red-600">{error}</p>}<button disabled={busy} className="rounded-xl bg-red-600 px-5 py-3 font-bold text-white disabled:opacity-50">{busy?'Creating...':'Create shop'}</button></form></div></main>}
